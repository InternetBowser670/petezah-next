import { Poppins, Geist_Mono } from "next/font/google";

export const poppins = Poppins({ weight: "700", subsets: ["latin"] });
export const geistMono = Geist_Mono({ subsets: ["latin"], weight: "400" });
